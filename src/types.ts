export interface SystemMessage {
  id: number;
  error_code: string;
  error_message: string;
  icm_error_solution: string;
  explanation: string;
  fix: string;
  service_desk: string;
  limited_data: string;
  last_updated: string;
}
